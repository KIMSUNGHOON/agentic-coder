# FastAPI and ASGI server
fastapi>=0.100.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Pydantic settings (separate from pydantic core)
pydantic-settings>=2.0.0

# Microsoft Agent Framework (includes openai, pydantic, httpx)
# Note: Using flexible version to avoid dependency conflicts
agent-framework>=1.0.0b1

# Database
sqlalchemy>=2.0.0

# Vector DB for semantic search (RAG Phase 3)
chromadb>=0.4.0

# Caching
redis>=5.0.0

# Additional dependencies
python-jose[cryptography]>=3.3.0
structlog>=24.0.0

# Phase 1 DeepAgent dependencies
aiofiles>=24.0.0  # Async file I/O for tool system
networkx>=3.0     # Knowledge graph for memory system

# LangChain ecosystem for dual framework support
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0
langgraph>=0.2.0

# DeepAgents framework for advanced middleware
deepagents>=0.1.0

# Agent Tools Phase 1 dependencies
tavily-python>=0.3.0  # Web search capability (requires Tavily API key)

# Testing (dev dependencies)
pytest>=7.0.0
pytest-asyncio>=0.21.0
