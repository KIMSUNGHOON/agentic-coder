"""API routes package.

This package contains all API route modules for the application.
"""

from fastapi import APIRouter

# Note: Individual route modules should be imported explicitly
# in the main application to avoid circular dependencies
